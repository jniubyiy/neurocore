[package]
name = "neurocore"
version = "0.1.0"
edition = "2021"

[dependencies]
faer = { version = "0.24.0", features = ["rayon"] }  # rayon для параллельных вычислений
once_cell = "1.19"
serde = { version = "1", features = ["derive"] }
serde_json = "1"